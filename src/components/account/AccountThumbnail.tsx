import styled from 'styled-components';

const AccountThumbnail = styled.img`
	border-radius: 50%;
	box-shadow: 0 1px 6px rgb(25 25 34 / 16%);
	display: inline-block;
	margin: 0;
	height: 4rem;
	overflow: hidden;
	position: relative;
	width: 4rem;
`;

export default AccountThumbnail;
